import React from 'react';

const Wishlist = () => {
    return (
        <div className="wishlist-container">
            <h1>My Wishlist</h1>
            {/* Here you can map through the wishlist items and display them */}
        </div>
    );
};

export default Wishlist;